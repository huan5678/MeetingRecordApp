{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults plus the folder picker for the storage-location setting.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
